{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @cloudleak/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "crons": [
    {
      "path": "/api/worker/run",
      "schedule": "0 0 * * *"
    }
  ]
}
